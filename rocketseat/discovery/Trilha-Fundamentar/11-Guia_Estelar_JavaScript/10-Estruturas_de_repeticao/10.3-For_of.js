console.log(`Aula 10.3-For_of

`)

// for...of

let name = 'Uener'
let names = ['Catia', 'David', 'Leonardo']

for (let char of name) {
  console.log(char)
}

for (let name of names) {
  console.log(name, name[0], name[1], name[2], name[3], name[4])
}