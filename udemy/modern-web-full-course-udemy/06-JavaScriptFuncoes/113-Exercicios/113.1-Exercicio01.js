function calculos(a, b) {
  let = 0;
  return `
  ${a} Somado com ${b} = ${let = a + b}
  ${a} Subtraído de ${b} = ${let = a - b}
  ${a} Multiplicado por ${b} = ${let = a * b}
  ${a} Dividido por ${b} = ${let = a / b} 
  `
}

console.log(calculos(2, 2))
console.log(calculos(3, 32))
console.log(calculos(5, 23))
console.log(calculos(20, 12))