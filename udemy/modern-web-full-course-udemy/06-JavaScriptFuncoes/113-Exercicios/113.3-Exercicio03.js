// Método Antigo
function exponencial(base, expoente) {
  let resultado = Math.pow(base, expoente);
  return resultado
}

console.log(exponencial(2, 3))

// Método ES6

pow = (base, expoente) => {
  return resultado = base ** expoente
}

console.log(pow(2, 8))