extends Node2D
class_name _11_resered_funtions


# Called when the node enters the scene tree for the first time.
func _ready(): #_ready é uma palavra reservada da linguagem
	pass # Pass utilizado quando a função está vazia. Não podemos deixar uma função vazia, pois resultará em erro


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta): # Basicamente a _process é uma função de update da cena.
#	print(delta)
	pass


# Tudo que form relacionado a física do jogo, deve ser processado na função _physics_process(delta):
func _physics_process(delta):
	pass
