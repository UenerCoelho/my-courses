// Manipulando conteúdo com innerHTML
// Modifica HTML interno do document

const element = document.querySelector('h1')

element.innerHTML = 'Title Changed | <p>innerHTML added here, with "innerHTML" we can transform texts in HTML</p>'