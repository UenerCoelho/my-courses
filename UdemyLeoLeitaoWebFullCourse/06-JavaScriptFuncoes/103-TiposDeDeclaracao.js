// Function Declaration
function soma (x, y) {
  return x + y;
};

// Function Expression
const sub = function (x, y) {
  return x - y;
};

// Named Function Expression
const mult = function mult(x, y) {
  return x * y;
};

