;(() => {
  const name = 'Uener'
  const age = 4

  if (age < 18) {
    console.log(
      `${name}, você é menor de idade, pois sua idade é: ${age}, que é menor que 18!`
    )
  } else {
    console.log(
      `${name}, você é maior de idade, pois sua idade é: ${age}, que é maior que 18!`
    )
  }
})()
